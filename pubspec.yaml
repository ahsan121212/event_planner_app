name: event_planner_admin
description: "Admin panel for the Event Planner app"
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter
  cupertino_icons: ^1.0.6
  provider: 6.1.2
  google_fonts: ^6.2.1
  responsive_framework: ^1.1.0
  flutter_svg: ^2.0.10
  shared_preferences: ^2.2.2
  go_router: ^13.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
